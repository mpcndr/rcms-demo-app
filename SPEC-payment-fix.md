# SPEC — แยกการชำระ "ค่าธรรมเนียม/ค่าปรับ" ออกจาก "การประกอบทุน"

ไฟล์เป้าหมาย: `index.html` (ไฟล์เดียว ไม่มี build step ไม่มี dependency)
ภาษาโค้ด: vanilla JS แบบ ES5 (ห้ามใช้ arrow function, template literal, optional chaining — โค้ดเดิมใช้ `function(){}` กับ `'a'+'b'` ทั้งไฟล์ ให้เขียนตามสไตล์เดิม)

---

## 1. ปัญหาที่ต้องแก้

Prototype ปัจจุบันมีปุ่ม **"ຊຳລະທຶນດ້ວຍ QR" / "Pay by QR"** อยู่ในหน้าผู้ประกอบการ (`vBizDash` บรรทัด ~1201 และ `vMobile` บรรทัด ~1244) โดยผูกเงื่อนไขไว้กับ `remain > 0` ซึ่ง `remain = e.cap - e.paid` คือ **ยอดทุนคงเหลือ**

**นี่ผิดตาม TOR** เอกสาร `RCMS_Executive_Summary.docx` ระบุ QR ไว้ในโมดูล Financial Transaction Management เท่านั้น:

> คำนวณ**ค่าธรรมเนียม**, รองรับการชำระเงินผ่าน QR/Mobile Banking/Counter, ตรวจสอบการชำระเงินแบบ Real-time, ออก E-Receipt, เชื่อมระบบบัญชี และติดตามสถานะ Unpaid/Paid

ส่วนการประกอบทุนอยู่คนละโมดูล (Capital Monitoring) และระบุว่า:

> กรณีที่วิสาหกิจชำระทุนมาเป็นเงินสด, มีหน้าจอให้**เจ้าหน้าที่ของ APB/MOIC** ป้อนข้อมูลการชำระทุน และ Upload เอกสารที่เกี่ยวข้อง

ผลคือปุ่มปัจจุบันสื่อผิด 2 ชั้น — ผู้ประกอบการไม่ได้จ่ายทุนด้วย QR และไม่ได้เป็นคนบันทึกการชำระทุนเอง

### กฎที่ต้องยึด

| | ค่าธรรมเนียม + ค่าปรับ | การประกอบทุน |
|---|---|---|
| ช่องทาง | QR / Mobile Banking / เคาน์เตอร์ | โอนเข้าบัญชี APB |
| ใครบันทึก | ระบบตรวจ Real-time เอง | เจ้าหน้าที่ APB |
| ต้องอนุมัติไหม | ไม่ต้อง | ต้องผ่าน Checker → Reviewer → Approver |
| ผลลัพธ์ | E-Receipt ทันที | ใบรับรองการฝากทุน |

---

## 2. งานที่ต้องทำ

### 2.1 เพิ่ม data model ใบแจ้งหนี้

เพิ่ม array ระดับบนสุดข้าง ๆ `seed()` ชื่อ `seedInv()` คืนค่าใบแจ้งหนี้ของวิสาหกิจ `ELN-2025-004871` (วิสาหกิจของ role `owner`)

โครงสร้างแต่ละใบ:

```js
{ id:'INV-2026-1180', ent:'ELN-2025-004871',
  kind:'fee',                   // 'fee' | 'penalty'
  title:{lo:'...',en:'...'},
  amt:850000, due:'25/08/2026',
  status:'PAID',                // 'PAID' | 'UNPAID'
  paidAt:'12/08/2025', receipt:'RCP-2025-0417' }
```

ใส่ไว้ 2 ใบ:
1. `INV-2026-1180` · kind `fee` · ค่าธรรมเนียมขึ้นทะเบียน · 850,000 · **PAID** · มีเลขใบเสร็จแล้ว
2. `INV-2026-1191` · kind `penalty` · ค่าปรับประกอบทุนล่าช้า · 2,400,000 · **UNPAID** · ครบกำหนด 25/08/2026

เก็บใน `S.invs` และ reset ใน `reset()` เหมือน `S.ents`

> หมายเหตุ: วิสาหกิจนี้สถานะเป็น PARTIAL ยังไม่ OVERDUE จริง แต่ให้ mock ใบค่าปรับไว้เพื่อสาธิต flow การจ่าย QR ได้ — ถ้าอยากให้สมเหตุสมผลกว่านี้ ให้เปลี่ยน title เป็น "ค่าธรรมเนียมแก้ไขทะเบียน" แทน แล้วคง kind เป็น `fee`

### 2.2 แก้หน้าผู้ประกอบการ (`vBizDash`)

**ลบ** ปุ่ม `data-soon="bizPayQR"` ที่ผูกกับ `remain > 0` ออก

**แทนที่ด้วย 2 การ์ดแยกกัน** วางต่อจาก `capClock(e)`:

**การ์ด A — วิธีชำระทุน** (แสดงเมื่อ `remain > 0`)
- หัวข้อ key ใหม่ `howToPay`
- แสดงเลขบัญชีรับทุน `e.acct` ตัวใหญ่ font `.num` พร้อมปุ่มคัดลอก
- ข้อความอธิบาย key ใหม่ `capTransferNote`:
  - lo: `ໂອນເຂົ້າບັນຊີຂ້າງເທິງ ແລ້ວເຈົ້າໜ້າທີ່ APB ຈະກວດສອບ ແລະ ບັນທຶກໃຫ້ ບໍ່ຕ້ອງອັບໂຫຼດເອກະສານເອງ`
  - en: `Transfer to the account above. An APB officer will verify the incoming funds and record the payment — you do not need to upload anything yourself.`
- **ห้ามมีปุ่มจ่าย** ในการ์ดนี้

**การ์ด B — ใบแจ้งหนี้** (แสดงเสมอ)
- key ใหม่ `myInvoices`
- ตารางคอลัมน์: เลขที่ / รายการ / จำนวน (LAK) / ครบกำหนด / สถานะ
- แถวที่ `status==='UNPAID'` มีปุ่ม **"ຊຳລະດ້ວຍ QR" / "Pay by QR"** (เปลี่ยน dict key `bizPayQR` เป็นค่านี้ เอาคำว่า "ທຶນ"/"capital" ออก)
- แถวที่ `PAID` แสดงลิงก์ `viewReceipt` แทน

### 2.3 เพิ่มหน้าจอ QR (modal)

เพิ่ม modal type ใหม่ `'qr'` ใน `vModal()` รับ `S.modal = {type:'qr', inv:<invoice id>}`

เนื้อหา:
- ชื่อรายการ + จำนวนเงินตัวใหญ่
- **QR code วาดด้วย SVG เอง ห้ามใช้ library** — ใช้วิธีเดียวกับ QR ปลอมในหน้า `vCert` (บรรทัด ~1180) คือ `linear-gradient` ลายตาราง หรือจะวาด `<rect>` เป็นกริดสุ่มแบบ deterministic ก็ได้ ขอแค่มี finder pattern 3 มุมให้ดูเหมือน QR จริง
- ข้อความ `scanNote`: lo `ສະແກນດ້ວຍແອັບທະນາຄານ ຫຼື Mobile Banking` / en `Scan with your banking app or Mobile Banking`
- ปุ่ม `simulatePaid` — lo `ຈຳລອງການຊຳລະສຳເລັດ` / en `Simulate successful payment` (ปุ่มสำหรับเดโมเท่านั้น ใส่ comment กำกับในโค้ด)
- ปุ่มยกเลิก

**เมื่อกด simulate:**
1. เปลี่ยน `status` → `'PAID'`, ใส่ `paidAt` = `'04/08/2026'`, gen `receipt` = `'RCP-2026-0' + (500 + index)`
2. **ห้ามสร้าง task เข้า `S.tasks`** — จุดสำคัญที่สุดของ spec นี้ ค่าธรรมเนียมไม่ผ่าน approval workflow
3. ปิด modal แล้วเปิด modal `'receipt'` ต่อทันที
4. `toast()` แจ้ง `paidOk` / `paidOkS` (lo: `ຊຳລະສຳເລັດ` / `ລະບົບກວດສອບອັດຕະໂນມັດ ບໍ່ຕ້ອງລໍການອະນຸມັດ`)

### 2.4 เพิ่ม E-Receipt (modal)

modal type `'receipt'` แสดงใบเสร็จอิเล็กทรอนิกส์ — สไตล์เดียวกับใบรับรองใน `vCert` แต่เล็กกว่า

ต้องมี: เลขที่ใบเสร็จ · ชื่อวิสาหกิจ · รายการ · จำนวนเงิน · วันเวลาที่ชำระ · ช่องทาง (QR / Mobile Banking) · ข้อความ `autoVerified` (lo: `ລະບົບກວດສອບການຊຳລະອັດຕະໂນມັດ` / en: `Payment verified automatically by the system`)

ปุ่ม: ดาวน์โหลด PDF (`data-soon`) และปิด

### 2.5 แก้หน้าการเงิน (`vFin`) ให้ดึงจาก `S.invs`

- แท็บ **fee** — filter `S.invs` ด้วย `kind==='fee'` แล้วต่อท้ายด้วยแถว mock เดิมของวิสาหกิจอื่น
- แท็บ **pen** — filter `kind==='penalty'` ต่อท้ายด้วย mock เดิม
- แท็บ **rec** — **แทนที่หน้าว่าง** ด้วยตารางใบเสร็จจริง filter `status==='PAID'` คอลัมน์: เลขที่ใบเสร็จ / วิสาหกิจ / รายการ / จำนวน / วันที่ชำระ / ปุ่มดูใบเสร็จ (เปิด modal `'receipt'`)

**เป้าหมาย:** พอผู้ประกอบการจ่าย QR แล้ว เจ้าหน้าที่ที่ล็อกอินเข้ามาต้องเห็นใบเสร็จนั้นในแท็บ E-Receipt ทันที โดยไม่มีใครต้องอนุมัติ — ใช้สาธิตความต่างจาก flow การชำระทุนได้ชัด

### 2.6 แก้ `vMobile`

ปุ่มเดิมบรรทัด ~1244 เปลี่ยนเป็นการ์ดใบแจ้งหนี้ค้างชำระ ถ้ามีใบ UNPAID → แสดงจำนวนเงินสีแดง + ปุ่มจ่าย QR (กดแล้วเปิด modal เดียวกัน) ถ้าไม่มี → แสดง `noInvoice`

### 2.7 เพิ่มบรรทัดใน `vDetail` ฝั่งเจ้าหน้าที่

ใต้การ์ด "ข้อมูลทะเบียน" เพิ่มบรรทัด key `feeStatus` แสดงว่าวิสาหกิจนี้มีใบแจ้งหนี้ค้างกี่ใบ เป็นเงินเท่าไหร่ กดแล้วไปหน้า `fin` แท็บ `pen`

---

## 3. ข้อกำหนดที่ห้ามพลาด

1. **dict ต้องสมมาตร** — ทุก key ที่เพิ่มต้องมีทั้ง `D.lo` และ `D.en` ปัจจุบันมีข้างละ 273 key เพิ่มแล้วต้องเท่ากัน ตรวจด้วย:
   ```
   node -e "..." # เทียบ Object.keys(D.lo) กับ Object.keys(D.en)
   ```
2. **ห้ามใช้ localStorage / sessionStorage** — state ทั้งหมดอยู่ใน `S` และรีเซ็ตด้วย `reset()`
3. **ห้ามเพิ่ม library** ทุกอย่างวาดเอง
4. **ปุ่มลัดเดิมต้องยังทำงาน** — `Alt+L` `Alt+M` `Alt+B` `Alt+Q` `Alt+R` `Alt+/` และ `Esc` ปิด modal
5. **ตัวเลขต้องผูกกัน** — ยอดในใบแจ้งหนี้ต้องตรงกันทุกที่ที่โผล่ (หน้าผู้ประกอบการ / หน้าการเงิน / ใบเสร็จ / มือถือ)
6. **สีต้องมาจาก CSS variable 4 ระดับเดิม** `--m-dark` `--m` `--m-light` `--m-lite` ห้าม hard-code สีใหม่ ยกเว้นสีสถานะที่มีอยู่แล้ว
7. **ตรวจ syntax ก่อนส่ง**:
   ```
   python3 -c "s=open('index.html',encoding='utf-8').read(); open('/tmp/a.js','w').write(s.split('<script>')[1].split('</script>')[0])"
   node --check /tmp/a.js
   ```

---

## 4. เกณฑ์ทดสอบ (ทำตามนี้แล้วต้องผ่านทุกข้อ)

1. เข้าด้วย `lao.agri` → เห็นการ์ด "วิธีชำระทุน" มีเลขบัญชี **ไม่มีปุ่มจ่าย**
2. เห็นตารางใบแจ้งหนี้ 2 แถว — PAID 1 UNPAID 1
3. กดจ่ายแถว UNPAID → ขึ้น QR → กด simulate → **ขึ้น E-Receipt ทันที ไม่มีขั้นรออนุมัติ**
4. สลับภาษา EN แล้วกลับ ລາວ → ทุกข้อความในหน้าใหม่เปลี่ยนครบ ไม่มีคำโผล่เป็น key ดิบ
5. ออกจากระบบ เข้าด้วย `head.moic` → เมนูการเงิน แท็บ E-Receipt → **เห็นใบเสร็จที่เพิ่งจ่าย**
6. เข้าด้วย `sivilay.a` → เปิดวิสาหกิจ ລາວກະສິກຳພັດທະນາ → บันทึกการชำระทุน → **flow เดิมต้องยังทำงานครบ 3 ขั้น ไม่ถูกกระทบ**
7. `Alt+R` รีเซ็ต → ใบแจ้งหนี้กลับเป็น UNPAID เหมือนเดิม
8. `node --check` ผ่าน

---

## 5. บริบทที่ควรรู้ก่อนแก้

- ไฟล์นี้เป็น prototype สำหรับ **นำเสนอชิงงาน** ไม่ใช่ production — ความชัดเจนตอนสาธิตสำคัญกว่าความสมบูรณ์ของโค้ด
- Role ในระบบ: `apb` (Checker) · `reviewer` · `approver` · `owner` (ผู้ประกอบการ)
- `S.ents[0]` คือวิสาหกิจของ role `owner` เสมอ
- โครงสร้าง view: `render()` เลือก view จาก `S.view` แล้วเรียก `bind()` ผูก event ใหม่ทุกครั้ง — **event ต้องผูกใน `bind()` เท่านั้น** ห้ามใช้ inline `onclick` ใน HTML string
- pattern การเปิด modal: ตั้ง `S.modal = {...}` แล้ว `render()`
- ปุ่มที่ยังไม่ทำจริงใช้ `data-soon="ข้อความ"` แล้วมันจะ toast ว่า "ยังไม่ได้ทำในเดโมนี้" อัตโนมัติ
